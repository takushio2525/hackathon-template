// sound_engine.pde — 音源エンジン（雛形）
// Processing で波形生成 / 音声再生 / 可視化を行う想定の空スケッチ。
// ライブラリや仕様は議論で確定後に追加する。

void setup() {
  size(640, 360);
  background(0);
  // ここに初期化処理を書く（シリアル接続，オーディオ出力の準備 等）
}

void draw() {
  // ここに毎フレームの処理を書く（音の生成，波形描画，状態表示 等）
}
