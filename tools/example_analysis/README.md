# example_analysis — データ解析スクリプトの例

収集したデータを集計・可視化するスクリプトを置く場所の例。

## 想定入力

- 実験で取得した生データ（CSV / WAV / 画像 / ログ等）

## 想定出力

- 統計量
- グラフ画像
- レポート用の表（Markdown / CSV）

## 実装

未実装。Python + `pandas` / `numpy` / `matplotlib` などを想定。
