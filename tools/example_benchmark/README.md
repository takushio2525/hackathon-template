# example_benchmark — 性能測定スクリプトの例

処理時間・スループット・エラー率などを測るスクリプトを置く場所の例。

## 想定入力

- ログファイル（CSV / JSON など）
- 計測対象のバイナリや API エンドポイント

## 想定出力

- 集計結果（CSV）
- グラフ画像（PNG / SVG）

## 実装

未実装。Python + `pandas` + `matplotlib` や、シェルスクリプトなど、
プロジェクトに合う道具で書く。
