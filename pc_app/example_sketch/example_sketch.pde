// example_sketch.pde — Processing スケッチの最小サンプル
//
// Processing は画像・音・シリアル通信などを簡単に扱える言語/環境です。
// マイコンとシリアルで接続し PC 側で可視化したいときや、音を鳴らす・
// グラフを描くといった用途に向いています。
//
// 不要ならこのフォルダごと削除してください。

void setup() {
  size(640, 360);
  background(0);
}

void draw() {
  // 毎フレームここが呼ばれる
}
